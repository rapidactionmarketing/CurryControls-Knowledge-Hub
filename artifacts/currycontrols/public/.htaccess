RewriteEngine On

# Keep real files and directories working normally.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Send client-side routes back to the React entry point.
RewriteRule ^ index.html [L]